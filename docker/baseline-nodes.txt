# One entry per line: <git-url> [git-ref]
# Leave the ref empty to track the repository default branch.
# These are seeded into ./data/custom_nodes on first start and never overwrite
# what is already there. Add or remove entries to fit your workflow.
#
# ComfyUI-Manager is NOT listed here: it is installed as a pip package from
# ComfyUI's manager_requirements.txt (see docker/Dockerfile) and comfy-cli
# enables it with --enable-manager. A git-cloned Manager is deliberately
# disabled by policy in that mode.
https://github.com/Kosinkadink/ComfyUI-VideoHelperSuite.git
https://github.com/rgthree/rgthree-comfy.git
https://github.com/pythongosssss/ComfyUI-Custom-Scripts.git
